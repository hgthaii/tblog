// API base (served by Go backend)
export const API_BASE = '/api';

// Backwards-compatible paths (fallbacks) served under /static when using Go server
export const JSON_PATH = '/static/data/json';
export const MARKDOWN_PATH = '/static/data/markdown';
